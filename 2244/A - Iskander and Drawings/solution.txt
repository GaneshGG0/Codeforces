t = int(input())
 
for _ in range(t):
    n = int(input())
    s = input()
 
    ans = 0
    cnt = 0
 
    for ch in s:
        if ch == '#':
            cnt += 1
        else:
            ans = max(ans, (cnt + 1) // 2)
            cnt = 0
 
    ans = max(ans, (cnt + 1) // 2)
 
    print(ans)